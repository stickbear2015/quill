feat(gates): complete Tier 1 gate ladder and reach a gate-clean tree

- GATE-2: add .github/workflows/pr-ci.yml (lint, full-suite coverage-floor,
  and characterization jobs) as the required PR pipeline
- GATE-5: enforce a hard 70% core+io coverage floor (now 72.28%); add
  pytest-cov to dev extras
- GATE-6: freeze MainFrame's 235-method public surface via
  quill/tools/ui_surface.py + a committed snapshot fixture and test, so
  the planned decomposition stays behavior-preserving
- GATE-7: add announcement-grammar conformance suite to Accessibility CI
  alongside the existing keyboard-trap, focus-order, and SR-transcript checks
- GATE-8: add a security-checks job running the hardened-XML check and the
  TLS, redaction, XML-bomb, zip-cap, sandbox, and OCR-allowlist suites
- CQ-18 (partial): format the whole first-party tree and clear auto-fixable
  lint (imports, zip strict=, pyupgrade, E402); defer the E501 backlog and
  add a vendored/distribution extend-exclude so the gate is scoped to source
- fix(core): autosave latest-snapshot ordering bug — the '-001' collision
  suffix sorted before the bare '.snap' name, so latest_autosave could
  return the older snapshot when two saves shared a microsecond stamp; every
  snapshot now carries a zero-padded counter so names sort in write order
- docs(plan): mark GATE-2/5/6/7/8 Done, set Tier 1 to 20/20 in the tracker,
  and add a continuation brief with the agreed Tier 4-then-2 sequence and
  no-collision parallelization guidance

Tree is gate-clean: ruff format/check pass, scoped mypy zero errors, full
suite 701 passed / 7 skipped.
